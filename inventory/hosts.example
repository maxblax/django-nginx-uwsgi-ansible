[all:vars]
ansible_user=ansible
ansible_ssh_private_key_file=~/.ssh/ansible_rsa
ansible_python_interpreter=/usr/bin/python3

[django_server]
production ansible_host=YOUR_SERVER_IP

# Optional: Add staging server if you want separate servers
# [django_server]
# staging ansible_host=STAGING_IP
# production ansible_host=PRODUCTION_IP
